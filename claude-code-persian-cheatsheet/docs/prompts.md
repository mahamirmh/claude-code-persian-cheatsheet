# پرامپت‌های آماده برای Claude

این پرامپت‌ها برای Claude معمولی و Claude Code قابل استفاده‌اند.

---

## تحلیل متن و صحت‌سنجی

```text
این متن را از نظر دقت، منطق، تناقض، اغراق و ریسک گمراه‌کردن مخاطب بررسی کن. خروجی را در سه بخش بده:
۱. موارد درست
۲. موارد مشکوک یا نیازمند منبع
۳. نسخه اصلاح‌شده
متن:
...
```

```text
نقش منتقد سخت‌گیر را بگیر. فرض کن می‌خواهی این ادعا را رد کنی. ضعیف‌ترین بخش‌های استدلال من را پیدا کن:
...
```

---

## تولید محتوا

```text
مثل یک استراتژیست محتوا عمل کن. برای موضوع زیر یک سناریوی ویدیویی بساز که هوک قوی، روایت مرحله‌ای، مثال ساده، جمع‌بندی و CTA داشته باشد:
موضوع: ...
مخاطب: ...
لحن: ...
طول ویدیو: ...
```

```text
این متن را برای اینستاگرام بازنویسی کن: کوتاه، کلیک‌خور، بدون اغراق، با هوک قوی در خط اول، سئو شده و مناسب مخاطب فارسی.
متن:
...
```

```text
برای این موضوع ۲۰ ایده تیتر/کاور بده. تیترها باید کنجکاوی ایجاد کنند، اما زرد و دروغین نباشند:
موضوع:
...
```

---

## کدنویسی

```text
این پروژه را بررسی کن و قبل از هر تغییری فقط ساختار، تکنولوژی‌ها، فایل‌های مهم و مسیر اجرای پروژه را توضیح بده.
```

```text
این باگ را مرحله‌به‌مرحله دیباگ کن. اول فرضیه‌ها را بگو، بعد فایل‌های مشکوک را بررسی کن، بعد کم‌ریسک‌ترین اصلاح را پیشنهاد بده:
باگ:
...
```

```text
کد زیر را refactor کن، اما رفتار خروجی نباید تغییر کند. خوانایی، نام‌گذاری، حذف تکرار و مدیریت خطا را بهتر کن:
...
```

---

## سئو

```text
برای محصول زیر توضیحات سئو شده بنویس. خروجی شامل عنوان سئو، متادیسکریپشن، توضیح کوتاه، توضیح بلند، مزایا، FAQ و کلمات کلیدی باشد.
محصول:
...
مخاطب:
...
لحن برند:
...
```

---

## گزارش و مقاله

```text
از متن زیر یک گزارش دانشگاهی منظم بساز. ساختار شامل چکیده، مقدمه، بدنه با تیترهای منطقی، جمع‌بندی و منابع پیشنهادی باشد. متن را رسمی، دقیق و بدون تکرار بنویس:
...
```

---

## ساخت اسلاید

```text
این متن را به اسلایدهای حرفه‌ای تبدیل کن. هر اسلاید باید فقط یک پیام اصلی داشته باشد. برای هر اسلاید بده:
عنوان
جمله کلیدی
۳ تا ۵ بولت کوتاه
پیشنهاد تصویر/آیکون
نوت ارائه‌دهنده
متن:
...
```
